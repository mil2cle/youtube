# 🎬 YouTube Content Assistant

**ภาษา:** [English](README.en.md) | [ไทย](README.md)

---

**YouTube Content Assistant** คือโปรเจกต์ Python แบบ modular ที่ออกแบบมาเพื่อเป็นผู้ช่วยในการสร้างและจัดการเนื้อหาสำหรับช่อง YouTube โดยใช้หลักการของ **Self-Improving System** ที่ระบบสามารถเรียนรู้และปรับปรุงตัวเองได้จากข้อมูล Performance ของวิดีโอ

โปรเจกต์นี้ถูกสร้างขึ้นเพื่อช่วยให้ YouTuber สามารถ:
-   **วิเคราะห์ข้อมูลเชิงลึก (Analytics):** ติดตาม Performance ของวิดีโอและของช่องโดยรวมผ่าน **YouTube Data & Analytics APIs**
-   **วางแผนเนื้อหา (Content Planning):** จัดการไอเดีย, สร้างปฏิทินเนื้อหา, และติดตามสถานะการผลิต
-   **วิจัยและติดตาม Trends (Research):** ค้นหาหัวข้อที่กำลังเป็นที่นิยมและวิเคราะห์คู่แข่ง
-   **ปรับปรุงกลยุทธ์อัตโนมัติ (Self-Improving Playbook):** สร้างกฎ (Rules) จากข้อมูล Performance เพื่อแนะนำกลยุทธ์ที่ดีที่สุดสำหรับช่อง

## ✨ อัพเดทล่าสุด: v1.5 - Dashboard Actions & UPSERT Fix!

เวอร์ชัน 1.5 เพิ่มความสามารถในการใช้งานผ่าน **Dashboard** โดยไม่ต้องใช้ Command Line:

-   **🔄 Actions / Sync Page:** หน้าใหม่สำหรับรันงานทั้งหมดผ่านปุ่มกดใน Dashboard
-   **Service Layer:** สร้าง `src/services/tasks.py` สำหรับจัดการ tasks ทั้งหมด
-   **UPSERT Fix:** แก้บั๊ก duplicate video insert - ตอนนี้จะ update ถ้ามีอยู่แล้ว แทนที่จะ error
-   **Deduplicate in-memory:** กรองข้อมูลซ้ำก่อนบันทึกลงฐานข้อมูล
-   **Skip bad records:** ข้ามและ log records ที่ parse ไม่ได้ แทนที่จะหยุดทั้งหมด
-   **Task locking:** ป้องกันการรัน task ซ้ำซ้อน

### v1.4 - Impressions & CTR Tracking

เวอร์ชัน 1.4 เพิ่มความสามารถในการติดตาม **Impressions** และ **CTR (Click-Through Rate)** ซึ่งเป็น metrics สำคัญสำหรับการวิเคราะห์ประสิทธิภาพ Thumbnail และ Title:

-   **เพิ่ม Impressions & CTR columns:** ในตาราง `daily_metrics` สำหรับเก็บข้อมูลรายวัน
-   **ดึงข้อมูลจาก YouTube Analytics API:** อัพเดท client ให้ดึง impressions และ impressionsClickThroughRate
-   **Dashboard แสดง CTR:** เพิ่มการแสดงผล Impressions, Avg CTR และ tab "Top Long Videos by CTR"
-   **Migration Script:** สร้าง `migrate_db.py` สำหรับอัพเกรดฐานข้อมูลเดิม
-   **Smoke Test Script:** สร้าง `smoke_test_youtube_ingestion.py` สำหรับตรวจสอบระบบก่อนใช้งาน

### Playbook Learning Module

ระบบยังคงมี **Playbook Learning Module** ซึ่งเป็นหัวใจของ Self-Improving System:
-   **เรียนรู้จากข้อมูลจริง:** วิเคราะห์ความสัมพันธ์ระหว่าง features ของวิดีโอ (เช่น ความยาว title, เวลาที่เผยแพร่, การใช้ keywords) กับ performance metrics (เช่น views, CTR)
-   **สร้างกฎภาษาไทยที่นำไปใช้ได้จริง:** สร้างกฎ (Rules) ที่เข้าใจง่ายในรูปแบบภาษาไทย เช่น "✅ ควรเผยแพร่วิดีโอช่วงเย็น (17:00-21:00)" พร้อมค่าความมั่นใจ (Confidence Score)
-   **ใช้ Interpretable Models:** เริ่มต้นด้วยโมเดลที่ตีความได้ง่ายอย่าง Logistic/Linear Regression และ Decision Trees
-   **บันทึกและจัดการกฎ:** กฎที่เรียนรู้จะถูกบันทึกลงฐานข้อมูลในตาราง `playbook_rules`

## 🚀 คุณสมบัติหลัก

-   **YouTube API Integration:** เชื่อมต่อกับ YouTube Data API v3 และ YouTube Analytics API v2
-   **Playbook Learning Module:** เรียนรู้จากข้อมูลและสร้างกฎภาษาไทยที่นำไปใช้ได้จริง
-   **Anime Research Module:** ดึงข้อมูลอนิเมะและข่าวสารจาก AniList และ RSS feeds ที่เป็นทางการ
-   **Modular Architecture:** แยกส่วนการทำงานหลัก (YouTube, Analytics, Content, Research, Playbook) ออกจากกัน
-   **Database-Driven:** ใช้ SQLite และ SQLAlchemy ในการจัดเก็บข้อมูลทั้งหมด
-   **Interactive Dashboard:** สร้างด้วย Streamlit สำหรับแสดงผลข้อมูลและจัดการระบบ
-   **CLI Scripts:** มี Command-line scripts สำหรับการตั้งค่า, ดึงข้อมูล, และรัน modules
-   **Self-Improving Loop:** หัวใจของระบบคือ **Playbook Module** ที่สามารถเรียนรู้จากข้อมูลจริงเพื่อสร้างกฎที่นำไปใช้ปรับปรุงกลยุทธ์ในอนาคต

## 🛠️ เทคโนโลยีที่ใช้

-   **Backend:** Python 3.11+
-   **Machine Learning:** Scikit-learn
-   **Google APIs:** `google-api-python-client`, `google-auth-oauthlib`
-   **Anime Data:** AniList GraphQL API, Anime News Network RSS
-   **Dashboard:** Streamlit, Matplotlib
-   **Database:** SQLite + SQLAlchemy (ORM)
-   **Data Processing:** Pandas, NumPy
-   **CLI & Display:** Rich, Typer, Click

## 📂 โครงสร้างโปรเจกต์ (อัพเดท)

```
/youtube-content-assistant
├── .secrets/                  # โฟลเดอร์เก็บ credentials (ถูก ignore โดย git)
│   ├── client_secrets.json    # (ต้องสร้างเอง) OAuth client secrets
│   └── token.json             # (สร้างอัตโนมัติ) OAuth token
├── configs/
│   └── default.yaml         # ไฟล์ตั้งค่าหลักของระบบ
├── dashboard/
│   └── app.py               # โค้ด Streamlit Dashboard
├── scripts/
│   ├── init_db.py           # Script สำหรับสร้าง/รีเซ็ตฐานข้อมูล (รองรับ --yes flag)
│   ├── migrate_db.py        # << ใหม่! Script สำหรับ migration ฐานข้อมูล
│   ├── smoke_test_youtube_ingestion.py # << ใหม่! Smoke test ก่อนใช้งาน
│   ├── run_all.py           # Script สำหรับรัน modules ทั้งหมด
│   ├── fetch_youtube.py     # Script สำหรับดึงข้อมูลจาก YouTube API (รวม impressions/CTR)
│   ├── validate_youtube_auth.py # Script สำหรับตรวจสอบและตั้งค่า OAuth
│   ├── fetch_research.py    # Script สำหรับดึงข้อมูล Anime Research
│   ├── entity_linker.py     # Script สำหรับจัดการ Entity Linking
│   ├── train_playbook.py    # Script สำหรับฝึกโมเดลและสร้างกฎ
│   └── report_weekly.py     # Script สำหรับสร้างรายงานสรุป
├── src/
│   ├── anime/                 # Anime Research Module
│   ├── db/                    # Database models, connection, repository
│   ├── modules/               # Business logic modules
│   ├── services/              # << ใหม่! Service Layer สำหรับ Dashboard
│   │   └── tasks.py             # Task management (sync, fetch, train)
│   ├── playbook/              # Playbook Learning Module
│   │   ├── __init__.py
│   │   ├── feature_extractor.py # ดึง features จากข้อมูลวิดีโอ
│   │   ├── model_trainer.py     # ฝึก ML models (sklearn)
│   │   └── rule_generator.py    # สร้างกฎภาษาไทยจาก model insights
│   ├── youtube/               # Module จัดการ YouTube API
│   └── utils/                 # Utilities (Config, Logger)
├── pyproject.toml             # Project metadata และ dependencies
├── requirements.txt           # รายการ Dependencies
└── README.md                  # ไฟล์นี้
```

## ⚙️ การติดตั้งและเริ่มต้นใช้งาน

### 1. Clone โปรเจกต์และติดตั้ง Dependencies

```bash
# Clone โปรเจกต์
git clone <your-repository-url>
cd youtube-content-assistant

# สร้างและ Activate Virtual Environment
python3 -m venv venv
source venv/bin/activate  # สำหรับ Linux/macOS
# .\venv\Scripts\activate  # สำหรับ Windows

# ติดตั้ง Dependencies
pip install -r requirements.txt
```

### 2. ตั้งค่า YouTube API (OAuth 2.0)

(ขั้นตอนเหมือนเดิม...)

### 3. ยืนยันตัวตนและสร้าง Token

(ขั้นตอนเหมือนเดิม...)

### 4. สร้างฐานข้อมูล

```bash
# สร้างฐานข้อมูลและตาราง
python scripts/init_db.py

# (Optional) สร้างฐานข้อมูลพร้อมข้อมูลตัวอย่าง (Sample Data)
python scripts/init_db.py --seed

# (Optional) รีเซ็ตฐานข้อมูลโดยไม่ต้องยืนยัน (สำหรับ CI/CD)
python scripts/init_db.py --reset --yes
```

### 4.1 Migration ฐานข้อมูลเดิม (สำหรับผู้ใช้เดิม)

ถ้าคุณมีฐานข้อมูลเดิมอยู่แล้ว ให้รัน migration เพื่อเพิ่ม columns ใหม่ (impressions, impressions_ctr):

```bash
# ตรวจสอบว่าต้อง migrate อะไรบ้าง
python scripts/migrate_db.py --check

# รัน migration
python scripts/migrate_db.py
```

### 4.2 Smoke Test (ตรวจสอบระบบ)

ก่อนเริ่มใช้งาน ให้รัน smoke test เพื่อตรวจสอบว่าระบบพร้อมใช้งาน:

```bash
# ตรวจสอบ schema, mapping, และ imports
python scripts/smoke_test_youtube_ingestion.py

# แสดงรายละเอียดเพิ่มเติม
python scripts/smoke_test_youtube_ingestion.py --verbose
```

### 5. ดึงข้อมูล

(ขั้นตอนเหมือนเดิม...)

### 6. ฝึก Playbook และสร้างกฎ (ใหม่!)

หลังจากมีข้อมูลในระบบแล้ว คุณสามารถเริ่มฝึกโมเดลเพื่อสร้าง Playbook ได้

```bash
# ฝึกโมเดลและสร้างกฎโดยใช้ข้อมูลตัวอย่าง (demo mode)
python scripts/train_playbook.py --demo

# ฝึกโมเดล Classification (default) จากข้อมูลจริงใน DB
python scripts/train_playbook.py

# ฝึกโมเดล Regression เพื่อทำนายยอดวิว (views)
python scripts/train_playbook.py --task regression --target views

# บันทึกกฎที่สร้างลงในฐานข้อมูล
python scripts/train_playbook.py --save-rules
```

### 7. สร้างรายงานสรุปประจำสัปดาห์ (ใหม่!)

สร้างไฟล์สรุปภาษาไทย (`.txt`) และไฟล์ข้อมูล (`.csv`) ประจำสัปดาห์

```bash
# สร้างรายงานสำหรับ 7 วันล่าสุด
python scripts/report_weekly.py

# สร้างรายงานพร้อม export ข้อมูลเป็น CSV
python scripts/report_weekly.py --csv

# สร้างรายงานโดยใช้ข้อมูลตัวอย่าง
python scripts/report_weekly.py --demo
```

### 8. รัน Dashboard

```bash
streamlit run dashboard/app.py
```

จากนั้นเปิดเบราว์เซอร์ไปที่ `http://localhost:8501` เพื่อดูผลลัพธ์

## 🔗 การทำงานของ Modules (อัพเดท)

1.  **Data Collection (รวบรวมข้อมูล):**
    -   **`fetch_youtube.py`** (ทำงานผ่าน `Scheduler` หรือ manual) จะใช้ `YouTubeClient` เพื่อดึงข้อมูลวิดีโอและ daily metrics จาก YouTube API และบันทึกลงฐานข้อมูล
    -   **`fetch_research.py`** จะใช้ `AniListClient` และ `RSSFeedParser` เพื่อดึงข้อมูลอนิเมะและข่าวสาร บันทึกลงตาราง `research_items`
2.  **Analysis & Learning (วิเคราะห์และเรียนรู้):**
    -   **`train_playbook.py`** จะใช้ `FeatureExtractor` เพื่อเตรียมข้อมูล, `PlaybookModelTrainer` เพื่อฝึกโมเดล, และ `ThaiRuleGenerator` เพื่อสร้างกฎภาษาไทย บันทึกลงตาราง `playbook_rules`
3.  **Suggestion & Planning (ให้คำแนะนำและวางแผน):**
    -   **`ContentModule`** จะใช้ข้อมูลจาก `ResearchModule` และ `PlaybookModule` เพื่อสร้าง **คำแนะนำไอเดีย**
4.  **Reporting (รายงานผล):**
    -   **`report_weekly.py`** จะดึงข้อมูลสรุป, วิดีโอยอดนิยม, และกฎที่ใช้งานอยู่ มาสร้างเป็นรายงานสรุปประจำสัปดาห์
5.  **Feedback Loop (วงจรการปรับปรุง):**
    -   ข้อมูล Performance ของวิดีโอใหม่จะถูกดึงเข้ามาในระบบ ทำให้วงจรการเรียนรู้ดำเนินต่อไปเรื่อยๆ

## 🚀 การพัฒนาต่อในอนาคต

-   **AI/ML Integration:** ใช้โมเดล Machine Learning ในการทำนาย Performance ของไอเดีย, วิเคราะห์ข้อความในคอมเมนต์, หรือสร้าง Title/Description อัตโนมัติ
-   **Alembic Migrations:** เพิ่มระบบจัดการ Schema Migration เพื่อให้การเปลี่ยนแปลงโครงสร้างฐานข้อมูลทำได้ง่ายขึ้น
-   **RESTful API:** สร้าง API ด้วย FastAPI เพื่อให้สามารถเชื่อมต่อกับระบบอื่นได้
-   **Testing:** เพิ่ม Unit Tests และ Integration Tests เพื่อรับประกันคุณภาพของโค้ด
