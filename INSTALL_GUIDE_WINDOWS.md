# 📖 คู่มือการติดตั้ง YouTube Content Assistant บน Windows 11

---

## 1. ความต้องการของระบบ

| รายการ | เวอร์ชั่นที่แนะนำ |
|---|---|
| **ระบบปฏิบัติการ** | Windows 10 หรือ Windows 11 |
| **Python** | 3.11+ |
| **Git** | 2.30+ |

---

## 2. วิธีการติดตั้ง (อัตโนมัติ)

วิธีที่ง่ายที่สุดคือการใช้ **Installation Script** ที่เตรียมไว้ให้

1. **ดาวน์โหลดโปรเจ็ค:**
   - ไปที่ [https://github.com/mil2cle/youtube](https://github.com/mil2cle/youtube)
   - คลิก `Code` -> `Download ZIP`
   - แตกไฟล์ ZIP ที่ดาวน์โหลดมา (เช่น `youtube-main.zip`)

2. **รันสคริปต์ติดตั้ง:**
   - เข้าไปในโฟลเดอร์ `youtube-main` ที่แตกไฟล์ออกมา
   - ดับเบิลคลิกที่ไฟล์ `install_windows.bat`

   ![Install Script](https://i.imgur.com/example.png)  *(ภาพตัวอย่าง)*

3. **ทำตามขั้นตอนในหน้าต่าง Command Prompt:**
   - สคริปต์จะตรวจสอบ Python และ Git
   - ถ้าไม่พบ จะแจ้งให้ติดตั้งก่อน
   - สคริปต์จะติดตั้ง Dependencies, สร้างฐานข้อมูล และสร้าง Shortcut ให้โดยอัตโนมัติ

4. **เสร็จสิ้น!**
   - เมื่อติดตั้งเสร็จ จะมีโฟลเดอร์ `youtube-content-assistant` อยู่ใน `C:\Users\<YourUser>`
   - ในโฟลเดอร์นั้นจะมี script สำหรับรันโปรแกรม

---

## 3. วิธีการใช้งาน

หลังจากติดตั้งเสร็จแล้ว ให้ไปที่โฟลเดอร์ `C:\Users\<YourUser>\youtube-content-assistant`

| ไฟล์ | คำอธิบาย |
|---|---|
| `run_dashboard.bat` | **(ตัวหลัก)** ดับเบิลคลิกเพื่อเปิด Dashboard |
| `fetch_research.bat` | ดึงข้อมูล Anime Research ใหม่ |
| `train_playbook.bat` | ฝึก Playbook Model จากข้อมูลที่มีอยู่ |

**ขั้นตอนการใช้งาน:**

1. ดับเบิลคลิก `run_dashboard.bat`
2. เบราว์เซอร์จะเปิดไปที่ `http://localhost:8501` โดยอัตโนมัติ
3. คุณจะเห็นหน้า Dashboard ของโปรแกรม

---

## 4. การตั้งค่า YouTube API (สำคัญมาก)

เพื่อให้โปรแกรมสามารถดึงข้อมูลจากช่อง YouTube ของคุณได้ คุณต้องตั้งค่า OAuth 2.0

1. **สร้าง Project ใน Google Cloud Console:**
   - ไปที่ [https://console.cloud.google.com/](https://console.cloud.google.com/)
   - สร้าง Project ใหม่

2. **เปิดใช้งาน API:**
   - ไปที่ `APIs & Services` -> `Library`
   - ค้นหาและเปิดใช้งาน `YouTube Data API v3`
   - ค้นหาและเปิดใช้งาน `YouTube Analytics API`

3. **สร้าง OAuth 2.0 Client ID:**
   - ไปที่ `APIs & Services` -> `Credentials`
   - คลิก `Create Credentials` -> `OAuth client ID`
   - เลือก `Application type` เป็น `Desktop app`
   - ตั้งชื่อแล้วคลิก `Create`

4. **ดาวน์โหลด `client_secrets.json`:**
   - ในหน้า Credentials จะเห็น Client ID ที่สร้างขึ้น
   - คลิกไอคอนดาวน์โหลด (Download JSON) ทางด้านขวา
   - จะได้ไฟล์ชื่อ `client_secrets.json`

5. **นำไฟล์ไปวางในโปรเจ็ค:**
   - ไปที่โฟลเดอร์ `C:\Users\<YourUser>\youtube-content-assistant`
   - สร้างโฟลเดอร์ใหม่ชื่อ `.secrets` (มีจุดนำหน้า)
   - นำไฟล์ `client_secrets.json` ที่ดาวน์โหลดมาไปวางในโฟลเดอร์ `.secrets`

   **โครงสร้างโฟลเดอร์:**
   ```
   youtube-content-assistant/
   ├── .secrets/
   │   └── client_secrets.json
   ├── dashboard/
   ├── scripts/
   └── ...
   ```

6. **ยืนยันตัวตนครั้งแรก:**
   - รัน `fetch_youtube.bat` (จะสร้างให้ในเวอร์ชั่นถัดไป) หรือรันคำสั่ง:
     ```bash
     cd C:\Users\<YourUser>\youtube-content-assistant
     call venv\Scripts\activate.bat
     python scripts/fetch_youtube.py --all
     ```
   - เบราว์เซอร์จะเปิดขึ้นมาให้คุณ Login และอนุญาตให้โปรแกรมเข้าถึงข้อมูล YouTube ของคุณ
   - หลังจากอนุญาตแล้ว โปรแกรมจะสร้างไฟล์ `youtube_token.json` ไว้ในโฟลเดอร์ `.secrets` สำหรับการเข้าใช้งานครั้งต่อไป

---

## 5. การแก้ไขปัญหา

- **`'python' is not recognized...`**: ยังไม่ได้ติดตั้ง Python หรือไม่ได้เลือก "Add Python to PATH" ตอนติดตั้ง
- **`'git' is not recognized...`**: ยังไม่ได้ติดตั้ง Git
- **`ModuleNotFoundError`**: ลองลบโฟลเดอร์ `venv` แล้วรัน `install_windows.bat` ใหม่อีกครั้ง
- **Dashboard ไม่แสดงข้อมูล**: ลองรัน `fetch_research.bat` และ `fetch_youtube.bat` ก่อน

---

หากมีคำถามเพิ่มเติม สามารถสอบถามได้เลยครับ
